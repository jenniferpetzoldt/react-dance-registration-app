import { all } from 'redux-saga/effects';

function* selectForm(action) {
    // try {
    //     yield call(axios.get, '/api/form');
    //     yield call(axios.get, '/api/form/' + this.state.formId);
    // };
};


export default registrationSaga;