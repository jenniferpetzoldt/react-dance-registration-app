const registrationReducer = (state = [], action) => {
    switch (action.type) {
        // case 'ADD_FORM_ID':
        //     return action.payload;
        // case 'SET_FORM_DATA':
        //     return action.payload;
        default:
            return state;
    }
};

export default registrationReducer;